# 03：默认开发者群自动加入

## 目标
按旧 Jirai 行为：获取当前用户群组后，若未加入 `grp_44b87c7b-00a6-4ef1-9980-0eddf3c7f06d` 且设置开启，则后台调用一次加入 API，并刷新本地群组缓存。不要显示成功/失败提醒。

## 依赖与边界
无新表。复用当前 `src/repositories/group-profile/members.ts:106-118` 的 `joinGroup` 和现有群组加载路径；只添加最小设置键/调用位置。必须避免每次渲染或重复加载重复请求。

## 原始代码绝对路径

- `/home/fulutang/Documents/Github/VRCX-jirai/src/coordinators/groupCoordinator.js`
- `/home/fulutang/Documents/Github/VRCX-0-jirai/src/repositories/group-profile/members.ts`
- `/home/fulutang/Documents/Github/VRCX-0-jirai/src/components/dialogs/group-dialog/useGroupDialogState.ts`

## 旧代码可复用分析
旧实现完整位于 `VRCX-jirai/src/coordinators/groupCoordinator.js:645-690`：先 `getGroups`，用 `some(group.id===defaultGroupId)` 判断，再 `joinGroup`，成功后重新拉群组。其固定 group ID、条件和刷新次序应原样保留；不要照搬 Pinia、Promise 链或 console 文案。

## 子代理引导
“找到当前‘当前用户群组加载完成’的单一位置；添加一个布尔配置和去重 guard。复用 groupProfileRepository，不加 toast/dialog，不修改群组详情的手动加入行为。确认默认开关值前报告旧 advanced setting 的默认值。”

## 检查标准
- 已加入：零 join 请求；
- 未加入且关闭：零 join 请求；
- 未加入且开启：一次 join，随后一次缓存刷新；
- join 失败不阻断原有群组加载；
- 重入/重复加载不重复 join。