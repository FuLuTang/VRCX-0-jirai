# 06：已跟踪非好友

## 目标
迁移旧 Jirai 的“追踪非好友”名单、侧栏/资料入口和定时资料刷新。表名与字段尽量保持
`{prefix}_tracked_nonfriends(user_id, display_name, added_at)`；数据归当前登录账户所有，但被追踪者不必是好友。

## 依赖与边界
依赖 04，供 08/09 使用。不得改动上游好友表、好友投影或互惠好友快照。只新增独立表/命令/仓库和 UI 引用层。

## 原始代码绝对路径

- `/home/fulutang/Documents/Github/VRCX-jirai/src/services/database/trackedNonFriends.js`
- `/home/fulutang/Documents/Github/VRCX-jirai/src/coordinators/nonFriendCoordinator.js`
- `/home/fulutang/Documents/Github/VRCX-jirai/src/stores/trackedNonFriends.js`
- `/home/fulutang/Documents/Github/VRCX-jirai/src/views/Sidebar/components/TrackedNonFriendsSidebar.vue`
- `/home/fulutang/Documents/Github/VRCX-0-jirai/src/repositories/userProfileRepository.ts`

## 旧代码可复用分析
旧 CRUD 在 `src/services/database/trackedNonFriends.js:5-64`，可直接保留表名、字段和 CRUD 语义；
`trackedNonFriends.js` store、`TrackedNonFriendsSidebar.vue`、`nonFriendCoordinator.js` 提供列表与刷新流程；
`updateLoop.js:37-40,92-95` 是一小时刷新节拍。Vue store、`dbVars` 和 raw SQL 不能搬，但 SQL schema/名称是迁移基准。

## 子代理引导
“先建 Rust owner-scoped CRUD，名称与旧表一致；再建 React repository/store 和最小侧栏。刷新调用当前用户资料 API，限制并发、取消和每小时节拍。不得将 tracked user 加入 FriendRecord、mutual graph 或好友通知。”

## 检查标准
- add/remove/list/isTracked/updateName 与旧字段兼容；
- owner A/B 数据隔离；
- 非好友 ID 可保存与读取；
- 刷新被限流、可取消，移除后不再刷新；
- 上游好友/互惠表无写入；
- 侧栏、资料菜单和空/错误态可用。