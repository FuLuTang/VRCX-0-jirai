# 01：功能来源审计修订

## 目标
只保留“FuLu糖福禄 作者”或“由 FuLuTang 合入 PR”的 Jirai 增量；把头像历史、Friend Log、通知、Activity、Gallery 等上游已有能力移入排除附录。

## 依赖与边界
无代码依赖；完成后才允许将某功能排入后续迁移。修改 `旧版功能清单.md` 及其来源附录，不删除用户 `{…}` 批注。

## 原始代码与历史绝对路径

- `/home/fulutang/Documents/Github/VRCX-jirai/docs/JIRAI_FEATURES.md`
- `/home/fulutang/Documents/Github/VRCX-jirai/.git/`（仅通过 Git 历史命令审计）

## 旧代码/历史分析
以 `VRCX-jirai/docs/JIRAI_FEATURES.md` 的“只列 jirai 特有”声明为初筛，再用 `git log --author='FuLu糖福禄|FuLuTang'`、合并 PR 及变更文件验证。状态分布的初版是 `137abf6f`（Copilot），后续 `cf942bd0` 为 FuLu糖福禄重构，保留并标明来源链。

## 子代理引导
“只读比对 Git 历史与 `JIRAI_FEATURES.md`；给每项填写：来源类别、首个 Jirai 提交、关键文件、是否保留。不得把上游功能因 Jirai 改过样式而误列为 Jirai 新功能；不得改代码。”

## 检查标准
- 每个保留项有提交 hash 与路径；
- 合入 PR 和作者本人提交明确区分；
- 上游已有项解释为何排除；
- Markdown 链接/路径可解析，批注保留。