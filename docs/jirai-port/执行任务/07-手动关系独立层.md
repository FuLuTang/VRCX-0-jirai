# 07：手动关系独立层

## 目标
以旧 Jirai 表和字段实现人工关系：
`{prefix}_manual_relations_MANUEL(user_id_a, user_id_b, relation_type, added_at)`，并把它作为当前互惠图的绿色覆盖边。

## 依赖与边界
依赖 01；可与 06 并行。不得迁移旧版全局 `dbVars`，不得向上游 `*_mutual_graph_links` 写人工边，也不得改变上游抓取/布局的事实数据。

## 原始代码绝对路径

- `/home/fulutang/Documents/Github/VRCX-jirai/src/services/database/manualRelations.js`
- `/home/fulutang/Documents/Github/VRCX-jirai/src/stores/manualRelations.js`
- `/home/fulutang/Documents/Github/VRCX-jirai/src/views/Charts/components/ManualRelationsDialog.vue`
- `/home/fulutang/Documents/Github/VRCX-jirai/src/views/Charts/components/MutualFriends.vue`
- `/home/fulutang/Documents/Github/VRCX-jirai/docs/vrcx_mcd.svg`
- `/home/fulutang/Documents/Github/VRCX-0-jirai/crates/persistence/src/database/schema.rs`
- `/home/fulutang/Documents/Github/VRCX-0-jirai/src/features/charts/mutual-friends/mutualFriendsGraphData.ts`

## 旧代码可复用分析
直接参考 `services/database/manualRelations.js:11-98`：pair ID 排序、`INSERT OR IGNORE`、按 `added_at` 倒序、按用户查找；
`stores/manualRelations.js:28-70` 的 `pairKey` 和缓存语义；`ManualRelationsDialog.vue` 的增删工作流；
`MutualFriends.vue` 的绿色覆盖渲染。尽量抄算法/字段，不抄 Vue/Pinia/raw SQL 调用。

## 子代理引导
“在 Rust persistence 增加独立表与 typed CRUD，保留旧名称；pair 必须规范排序且拒绝相同 ID。前端图数据在构建完成的 mutual graph 上叠加 `{kind:'manual', addedAt}` 边，并有图例。不要编辑上游 snapshot 表或写入关系推荐。”

## 检查标准
- A/B 与 B/A 同一关系，重复 add 幂等；
- owner 隔离、删除、按用户检索、added_at 正确；
- 绿色边可见、可访问且和观测边可区分；
- 观测 graph snapshot 的内容/计数不改变；
- 所有 Rust schema/command、repository、组件测试通过。