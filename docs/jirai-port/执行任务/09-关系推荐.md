# 09：关系推荐

## 目标
迁移 Jirai 的共处/访问类型/创建者/重叠/缺席等关系推测，提供可解释建议、接受为手动关系和本会话忽略。

## 依赖与边界
必须等待 06（非好友）、07（手动关系）和 08（自动资料/历史输入）；读取当前 Feed、GameLog、互惠图，不改写它们。推荐不是事实好友关系。

## 原始代码绝对路径

- `/home/fulutang/Documents/Github/VRCX-jirai/src/stores/manualRelations.js`
- `/home/fulutang/Documents/Github/VRCX-jirai/src/services/database/manualRelations.js`
- `/home/fulutang/Documents/Github/VRCX-jirai/src/views/Charts/components/ManualRelationsDialog.vue`
- `/home/fulutang/Documents/Github/VRCX-jirai/src/coordinators/gameLogCoordinator.js`
- `/home/fulutang/Documents/Github/VRCX-jirai/src/coordinators/userCoordinator.js`
- `/home/fulutang/Documents/Github/VRCX-0-jirai/src/features/charts/relationshipHistory.ts`

## 旧代码可复用分析
重点抄 `stores/manualRelations.js:80-376` 的候选集合、pair key、分段让出和评分说明；
`services/database/manualRelations.js:102-172` 的三源查询结构（GameLog、feed GPS、online/offline、旧 mutual snapshot）；
`ManualRelationsDialog.vue` 的显示/接受/忽略；`gameLogCoordinator.js`、`userCoordinator.js` 的提示触发。先逐项把旧权重写成测试夹具，再决定是否原样保留。

## 子代理引导
“先写纯 Rust/TS 确定性评分函数和 fixtures，不写 UI。输入显式包含每一条证据及其 owner；同一对用户去重、手动边排除、ignore 仅 session。只有测试锁定旧行为后才接入图/UI。不得用前端全表扫描。”

## 检查标准
- 对固定旧数据夹具复现预期排序/分数；
- private/无效位置、重复、自己、缺少快照可解释处理；
- 不修改原始 Feed/GameLog/mutual 数据；
- 接受建议只调用 07 的 manual relation；
- 忽略不落库（除非用户以后明确要求）；
- 可取消、有限时/数量上限。